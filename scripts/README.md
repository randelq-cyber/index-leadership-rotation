# Scripts

Place project-specific extraction, analysis, or helper scripts here.

Prefer reusing `/home/yqin/repos/stock_trend_rq` for generic market-data download infrastructure before adding new download logic.

Scripts in this project should read data paths from local config rather than hardcoding raw data into the repo.

When running Python, use `/home/yqin/repos/stock_trend_rq/.venv/bin/python` instead of global Python.
